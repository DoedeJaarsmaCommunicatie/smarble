{% extends 'layouts/app.twig' %}

{% block content %}
    <main class="container mx-auto lg:flex py-8 px-4 lg:px-0">
        <aside class="lg:w-1/2 lg:pr-8">
            <img src="{{ post.thumbnail('large') }}" />
        </aside>
        <section class="lg:w-1/2">
            <h1>{{ post.title }}</h1>
            {{ post.content }}
        </section>
    </main>
{% endblock %}
